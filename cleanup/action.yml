# SPDX-License-Identifier: EUPL-1.2
name: 'Cleanup ZAD Deployment'
description: 'Remove a ZAD deployment and optionally clean up GitHub resources (environments, deployments, container images)'
author: 'RijksICTGilde'

branding:
  icon: 'trash-2'
  color: 'red'

inputs:
  api-key:
    description: 'ZAD API key (ZAD_API_KEY secret)'
    required: true
  project-id:
    description: 'ZAD project identifier (e.g., regel-k4c)'
    required: true
  deployment-name:
    description: 'Name of the deployment to delete'
    required: true
  delete-github-env:
    description: 'Delete the GitHub environment (requires github-admin-token)'
    required: false
    default: 'false'
  delete-github-deployments:
    description: 'Delete GitHub deployments for this environment (requires github-token with deployments:write)'
    required: false
    default: 'false'
  delete-container:
    description: 'Delete the container image from GHCR'
    required: false
    default: 'false'
  container-org:
    description: 'Organization owning the container (for image deletion)'
    required: false
    default: ''
  container-name:
    description: 'Container package name (for image deletion)'
    required: false
    default: ''
  container-tag:
    description: 'Container tag to delete (for image deletion)'
    required: false
    default: ''
  containers:
    description: >-
      JSON array of containers to delete:
      [{"org": "...", "name": "...", "tag": "..."}].
      Takes precedence over container-org/container-name/container-tag.
    required: false
    default: ''
  github-token:
    description: 'GitHub token for deployment/PR operations (defaults to automatic token)'
    required: false
    default: '${{ github.token }}'
  github-admin-token:
    description: 'GitHub token for environment deletion (needs repo admin permission, typically GITHUB_ADMIN_TOKEN secret)'
    required: false
    default: ''
  api-base-url:
    description: 'ZAD Operations Manager API base URL'
    required: false
    default: 'https://operations-manager.rig.prd1.gn2.quattro.rijksapps.nl/api'
  task-timeout:
    description: 'Maximum time in seconds to wait for async task completion'
    required: false
    default: '300'
  task-poll-interval:
    description: 'Seconds between task status polls'
    required: false
    default: '3'
  delete-pr-comment:
    description: 'Delete the deploy PR comment (requires github-token with pull-requests:write)'
    required: false
    default: 'true'
  comment-header:
    description: 'Header of the deploy comment to find and delete (default: "## 🚀 Preview Deployment")'
    required: false
    default: '## 🚀 Preview Deployment'
  skip-bot-prs:
    description: 'Skip cleanup for PRs created by bots (dependabot, renovate, pre-commit-ci, etc.)'
    required: false
    default: 'true'
  max-retries:
    description: 'Maximum number of retries for transient ZAD API errors (0 to disable)'
    required: false
    default: '3'
  retry-delay:
    description: 'Initial retry delay in seconds (doubles each retry)'
    required: false
    default: '2'

outputs:
  zad-deleted:
    description: 'Whether the ZAD deployment was deleted'
    value: ${{ steps.delete-zad.outputs.deleted }}
  github-env-deleted:
    description: 'Whether the GitHub environment was deleted'
    value: ${{ steps.delete-github-env.outputs.deleted }}
  github-deployments-deleted:
    description: 'Whether GitHub deployments were deleted'
    value: ${{ steps.delete-github-deployments.outputs.deleted }}
  container-deleted:
    description: 'Whether the container image was deleted'
    value: ${{ steps.delete-container.outputs.deleted }}
  pr-comment-deleted:
    description: 'Whether the PR comment was deleted'
    value: ${{ steps.delete-pr-comment.outputs.deleted }}
  skipped:
    description: 'Whether cleanup was skipped due to bot PR detection'
    value: ${{ steps.bot-check.outputs.skipped == 'true' && 'true' || 'false' }}

runs:
  using: 'composite'
  steps:
    - name: Check for bot PR
      id: bot-check
      if: inputs.skip-bot-prs == 'true' && (github.event_name == 'pull_request' || github.event_name == 'pull_request_target')
      shell: bash
      env:
        PR_AUTHOR: ${{ github.event.pull_request.user.login }}
        PR_AUTHOR_TYPE: ${{ github.event.pull_request.user.type }}
      run: |
        KNOWN_BOTS=("dependabot[bot]" "renovate[bot]" "pre-commit-ci[bot]" "github-actions[bot]")
        if [ "$PR_AUTHOR_TYPE" = "Bot" ]; then
          echo "::notice::Skipping: PR author '$PR_AUTHOR' is a bot (type: $PR_AUTHOR_TYPE)"
          echo "skipped=true" >> "$GITHUB_OUTPUT"
          exit 0
        fi
        for bot in "${KNOWN_BOTS[@]}"; do
          if [ "$PR_AUTHOR" = "$bot" ]; then
            echo "::notice::Skipping: PR author '$PR_AUTHOR' is a known bot"
            echo "skipped=true" >> "$GITHUB_OUTPUT"
            exit 0
          fi
        done
        echo "skipped=false" >> "$GITHUB_OUTPUT"

    - name: Install zad-cli
      if: steps.bot-check.outputs.skipped != 'true'
      shell: bash
      run: |
        command -v uv >/dev/null 2>&1 || curl -LsSf https://astral.sh/uv/install.sh | sh
        uv tool install git+https://github.com/RijksICTGilde/zad-cli.git

    - name: Delete ZAD Deployment
      id: delete-zad
      if: steps.bot-check.outputs.skipped != 'true'
      shell: bash
      env:
        ZAD_API_KEY: ${{ inputs.api-key }}
        ZAD_API_URL: ${{ inputs.api-base-url }}
        ZAD_PROJECT_ID: ${{ inputs.project-id }}
        ZAD_MAX_RETRIES: ${{ inputs.max-retries }}
        ZAD_RETRY_DELAY: ${{ inputs.retry-delay }}
        ZAD_TASK_TIMEOUT: ${{ inputs.task-timeout }}
        ZAD_TASK_POLL_INTERVAL: ${{ inputs.task-poll-interval }}
        DEPLOYMENT_NAME: ${{ inputs.deployment-name }}
        NO_COLOR: '1'
      run: |
        echo "Deleting ZAD deployment: $DEPLOYMENT_NAME from project: $ZAD_PROJECT_ID"

        RESULT=$(zad --output json deployment delete "$DEPLOYMENT_NAME" --yes --ignore-not-found) && ZAD_EXIT=0 || ZAD_EXIT=$?

        if [ "$ZAD_EXIT" -eq 0 ]; then
          REASON=$(echo "$RESULT" | jq -r '.reason // empty' 2>/dev/null)
          if [ "$REASON" = "not_found" ]; then
            echo "ZAD deployment not found (already deleted?)"
            echo "deleted=false" >> "$GITHUB_OUTPUT"
          else
            echo "ZAD deployment deleted successfully"
            echo "deleted=true" >> "$GITHUB_OUTPUT"
          fi
        else
          # Parse structured error from CLI JSON output (stdout)
          STATUS_CODE=$(echo "$RESULT" | jq -r '.status_code // 0' 2>/dev/null || echo "0")
          if [ "$STATUS_CODE" = "0" ] || [ "$STATUS_CODE" = "null" ]; then
            echo "::warning::Unable to connect to ZAD API - network issue or API unavailable"
          elif [ "$STATUS_CODE" = "401" ]; then
            echo "::warning::Authentication failed (HTTP 401) - verify ZAD_API_KEY is correct"
          elif [ "$STATUS_CODE" = "403" ]; then
            echo "::warning::Access denied (HTTP 403) - API key may lack permission for project '$ZAD_PROJECT_ID'"
          else
            ERROR_MSG=$(echo "$RESULT" | jq -r '.error // "unknown error"' 2>/dev/null)
            echo "::warning::Failed to delete ZAD deployment (HTTP $STATUS_CODE): $ERROR_MSG"
          fi
          echo "deleted=false" >> "$GITHUB_OUTPUT"
        fi

    - name: Delete GitHub Deployments
      id: delete-github-deployments
      if: steps.bot-check.outputs.skipped != 'true' && inputs.delete-github-deployments == 'true'
      shell: bash
      env:
        GH_TOKEN: ${{ inputs.github-token }}
        DEPLOYMENT_NAME: ${{ inputs.deployment-name }}
        GITHUB_REPOSITORY: ${{ github.repository }}
      run: |
        echo "Deleting GitHub deployments for environment: $DEPLOYMENT_NAME"

        # Get all deployments for this environment
        DEPLOYMENTS=$(gh api "repos/$GITHUB_REPOSITORY/deployments?environment=$DEPLOYMENT_NAME" --jq '.[].id' 2>/dev/null || echo "")

        if [ -z "$DEPLOYMENTS" ]; then
          echo "No GitHub deployments found for environment: $DEPLOYMENT_NAME"
          echo "deleted=false" >> "$GITHUB_OUTPUT"
          exit 0
        fi

        DELETED_COUNT=0
        for DEPLOYMENT_ID in $DEPLOYMENTS; do
          echo "Deleting deployment: $DEPLOYMENT_ID"

          # First, mark deployment as inactive
          gh api "repos/$GITHUB_REPOSITORY/deployments/$DEPLOYMENT_ID/statuses" \
            -X POST \
            -f state=inactive \
            2>/dev/null || true

          # Then delete the deployment
          if gh api "repos/$GITHUB_REPOSITORY/deployments/$DEPLOYMENT_ID" -X DELETE 2>/dev/null; then
            echo "Deleted deployment: $DEPLOYMENT_ID"
            DELETED_COUNT=$((DELETED_COUNT + 1))
          else
            echo "Failed to delete deployment: $DEPLOYMENT_ID (may already be deleted)"
          fi
        done

        echo "Deleted $DELETED_COUNT GitHub deployment(s)"
        if [ "$DELETED_COUNT" -gt 0 ]; then
          echo "deleted=true" >> "$GITHUB_OUTPUT"
        else
          echo "deleted=false" >> "$GITHUB_OUTPUT"
        fi

    - name: Delete GitHub Environment
      id: delete-github-env
      if: steps.bot-check.outputs.skipped != 'true' && inputs.delete-github-env == 'true' && inputs.github-admin-token != ''
      shell: bash
      env:
        GH_TOKEN: ${{ inputs.github-admin-token }}
        DEPLOYMENT_NAME: ${{ inputs.deployment-name }}
        GITHUB_REPOSITORY: ${{ github.repository }}
      run: |
        echo "Deleting GitHub environment: $DEPLOYMENT_NAME"

        # URL-encode the environment name
        ENCODED_ENV=$(echo "$DEPLOYMENT_NAME" | jq -sRr @uri)

        # Try to delete and capture the response
        HTTP_RESPONSE=$(gh api "repos/$GITHUB_REPOSITORY/environments/$ENCODED_ENV" \
          -X DELETE \
          --include \
          2>&1) || true

        # Check if it was successful (204 No Content) or environment didn't exist (404)
        if echo "$HTTP_RESPONSE" | grep -q "HTTP/2 204\|HTTP/1.1 204"; then
          echo "GitHub environment '$DEPLOYMENT_NAME' deleted successfully"
          echo "deleted=true" >> "$GITHUB_OUTPUT"
        elif echo "$HTTP_RESPONSE" | grep -q "HTTP/2 404\|HTTP/1.1 404"; then
          echo "GitHub environment '$DEPLOYMENT_NAME' does not exist (already deleted?)"
          echo "deleted=false" >> "$GITHUB_OUTPUT"
          echo "reason=not_found" >> "$GITHUB_OUTPUT"
        elif echo "$HTTP_RESPONSE" | grep -q "HTTP/2 403\|HTTP/1.1 403"; then
          echo "::error::Permission denied: github-admin-token lacks permission to delete environments"
          echo "::error::Ensure the token has 'repo' scope or is a GitHub App with 'administration:write' permission"
          echo "deleted=false" >> "$GITHUB_OUTPUT"
          echo "reason=permission_denied" >> "$GITHUB_OUTPUT"
        else
          echo "::warning::Failed to delete GitHub environment '$DEPLOYMENT_NAME'"
          echo "Response: $HTTP_RESPONSE"
          echo "deleted=false" >> "$GITHUB_OUTPUT"
          echo "reason=unknown" >> "$GITHUB_OUTPUT"
        fi

    - name: Delete Container Image
      id: delete-container
      if: >-
        steps.bot-check.outputs.skipped != 'true'
        && inputs.delete-container == 'true'
        && (inputs.containers != ''
        || (inputs.container-org != ''
        && inputs.container-name != ''
        && inputs.container-tag != ''))
      shell: bash
      env:
        GH_TOKEN: ${{ inputs.github-token }}
        CONTAINER_ORG: ${{ inputs.container-org }}
        CONTAINER_NAME: ${{ inputs.container-name }}
        CONTAINER_TAG: ${{ inputs.container-tag }}
        CONTAINERS: ${{ inputs.containers }}
      run: |
        # Build unified container list (JSON array)
        if [ -n "$CONTAINERS" ]; then
          # Multi-container mode: validate JSON
          if ! echo "$CONTAINERS" | jq empty 2>/dev/null; then
            echo "Error: containers input is not valid JSON"
            exit 1
          fi

          CONTAINER_COUNT=$(echo "$CONTAINERS" | jq 'length')
          if [ "$CONTAINER_COUNT" -eq 0 ]; then
            echo "Error: containers array must contain at least one entry"
            exit 1
          fi

          # Validate each entry has org, name, and tag
          INVALID=$(echo "$CONTAINERS" | jq -r \
            '.[] | select(.org == null or .org == ""
            or .name == null or .name == ""
            or .tag == null or .tag == "")
            | "missing org, name, or tag"' | head -n1)
          if [ -n "$INVALID" ]; then
            echo "Error: each container must have non-empty 'org', 'name', and 'tag'"
            exit 1
          fi

          CONTAINER_LIST="$CONTAINERS"
        else
          # Single-container mode: build JSON array from individual inputs
          CONTAINER_LIST=$(jq -n --arg org "$CONTAINER_ORG" --arg name "$CONTAINER_NAME" --arg tag "$CONTAINER_TAG" \
            '[{org: $org, name: $name, tag: $tag}]')
        fi

        # Validate all entries
        BAD_ORGS=$(echo "$CONTAINER_LIST" | jq -r '.[].org' | grep -vE '^[a-zA-Z0-9._-]+$' || true)
        if [ -n "$BAD_ORGS" ]; then
          echo "Error: container org(s) contain invalid characters (allowed: a-z, A-Z, 0-9, ., _, -)"
          exit 1
        fi
        BAD_NAMES=$(echo "$CONTAINER_LIST" | jq -r '.[].name' | grep -vE '^[a-zA-Z0-9._-]+$' || true)
        if [ -n "$BAD_NAMES" ]; then
          echo "Error: container name(s) contain invalid characters (allowed: a-z, A-Z, 0-9, ., _, -)"
          exit 1
        fi
        # Container tags: check for dangerous characters (quotes, backticks, backslashes)
        BAD_TAGS=$(echo "$CONTAINER_LIST" | jq -r '.[].tag' | grep -E '["`'\''\\]' || true)
        if [ -n "$BAD_TAGS" ]; then
          echo "Error: container tag(s) contain dangerous characters (quotes, backticks, backslashes not allowed)"
          exit 1
        fi

        # Process each container (best-effort: one failure does not stop others)
        ANY_DELETED=false
        ENTRY_COUNT=$(echo "$CONTAINER_LIST" | jq 'length')

        for i in $(seq 0 $((ENTRY_COUNT - 1))); do
          C_ORG=$(echo "$CONTAINER_LIST" | jq -r ".[$i].org")
          C_NAME=$(echo "$CONTAINER_LIST" | jq -r ".[$i].name")
          C_TAG=$(echo "$CONTAINER_LIST" | jq -r ".[$i].tag")

          echo "Deleting container image: ghcr.io/$C_ORG/$C_NAME:$C_TAG"

          # Get all versions of the package and filter by tag using jq safely
          VERSIONS=$(gh api "orgs/$C_ORG/packages/container/$C_NAME/versions" 2>/dev/null | \
            jq -r --arg tag "$C_TAG" '.[] | select(.metadata.container.tags | index($tag)) | .id' 2>/dev/null || echo "")

          if [ -z "$VERSIONS" ]; then
            echo "Container image not found or no matching tag: ghcr.io/$C_ORG/$C_NAME:$C_TAG"
            continue
          fi

          for VERSION_ID in $VERSIONS; do
            echo "Deleting version: $VERSION_ID"

            # Capture both stdout and stderr to detect "last tagged version" error
            DELETE_OUTPUT=$(gh api "orgs/$C_ORG/packages/container/$C_NAME/versions/$VERSION_ID" -X DELETE 2>&1)
            DELETE_RESULT=$?

            if [ $DELETE_RESULT -eq 0 ]; then
              echo "Deleted container version: $VERSION_ID"
              ANY_DELETED=true
            else
              echo "$DELETE_OUTPUT"

              # Check if this is the "last tagged version" error
              if echo "$DELETE_OUTPUT" | grep -q "You cannot delete the last tagged version"; then
                echo "This is the last tagged version. Deleting entire package instead..."

                # Delete the entire package
                if gh api "orgs/$C_ORG/packages/container/$C_NAME" -X DELETE 2>&1; then
                  echo "Successfully deleted entire package: $C_NAME"
                  ANY_DELETED=true
                else
                  echo "::warning::Failed to delete entire package: $C_NAME"
                fi
              else
                echo "::warning::Failed to delete version: $VERSION_ID for ghcr.io/$C_ORG/$C_NAME:$C_TAG"
              fi
            fi
          done
        done

        echo "deleted=$ANY_DELETED" >> "$GITHUB_OUTPUT"

    - name: Delete PR Comment
      id: delete-pr-comment
      if: steps.bot-check.outputs.skipped != 'true' && inputs.delete-pr-comment == 'true' && github.event_name == 'pull_request'
      shell: bash
      env:
        GH_TOKEN: ${{ inputs.github-token }}
        COMMENT_HEADER: ${{ inputs.comment-header }}
        PR_NUMBER: ${{ github.event.pull_request.number }}
        GITHUB_REPOSITORY: ${{ github.repository }}
      run: |
        # Find all comments matching the header (covers both multi-component
        # single comment and multiple single-component comments)
        COMMENT_IDS=$(gh api "repos/${GITHUB_REPOSITORY}/issues/${PR_NUMBER}/comments" \
          --jq "[.[] | select(.body | startswith(\"${COMMENT_HEADER}\")) | .id]" \
          2>/dev/null || echo "[]")

        COUNT=$(echo "$COMMENT_IDS" | jq 'length')
        if [ "$COUNT" -eq 0 ]; then
          echo "No existing deploy comment(s) found with header: ${COMMENT_HEADER}"
          echo "deleted=false" >> "$GITHUB_OUTPUT"
          exit 0
        fi

        echo "Found $COUNT deploy comment(s) to delete"
        DELETED=0
        for COMMENT_ID in $(echo "$COMMENT_IDS" | jq -r '.[]'); do
          echo "Deleting PR comment (ID: $COMMENT_ID)"
          if gh api "repos/${GITHUB_REPOSITORY}/issues/comments/${COMMENT_ID}" -X DELETE 2>/dev/null; then
            echo "PR comment deleted successfully"
            DELETED=$((DELETED + 1))
          else
            echo "::warning::Failed to delete PR comment $COMMENT_ID"
          fi
        done
        if [ "$DELETED" -gt 0 ]; then
          echo "deleted=true" >> "$GITHUB_OUTPUT"
        else
          echo "deleted=false" >> "$GITHUB_OUTPUT"
        fi
